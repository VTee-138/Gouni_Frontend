// import { get } from "../common/apiClient";

// const PATH_CATEGORY = "/category";
// const getCategories = async (pageNumber, limit, category) => {
//   return await get(
//     PATH_CATEGORY + `?page=${pageNumber}&limit=${limit}&category=${category}`
//   );
// };
// const getCategoryById = async (id) => {
//   return await get(PATH_CATEGORY + `/${id}`);
// };

// export { getCategories, getCategoryById };
